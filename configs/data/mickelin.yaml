# Mickelin GNS vorticity ensemble (760 runs, 128x256 equiangular grid).
# Fields: vorticity (1 channel).
# Trajectory: 200 snapshots.
data:
  _target_: fots.data.zarr_dataset.ZarrDataModule
  root: /iopsstor/scratch/cscs/tmuser/PDEDatasets/SphericalPDEs/mickelin-gns-v2
  dataset_name: mickelin
  time_steps_per_run: 200
  dim_in: 1
  dim_out: 1
  spatial_resolution: [128, 256]
  grid: equiangular
  field_names: [vorticity]
  batch_size: 4
  n_steps_input: 4
  n_steps_output: 1
  num_workers: 8
