#-- list of packages (or group of packages) to compile for this experiment.
#   gfd pulls in the GFD core; exch2 is required for the cubed sphere; gmredi
#   exposes the sweep knob; ggl90 provides vertical mixing; diagnostics is
#   compiled in but disabled at runtime (we read state dumps directly).
exch2
gfd
gmredi
ggl90
diagnostics
